import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'flex-end',
    height: 24,
    gap: 3,
    position: 'absolute',
    left: 13,
    bottom: 13,
  },
  bar: {
    width: 4,
    backgroundColor: '#ffffff',
    borderRadius: 2,
  },
});
