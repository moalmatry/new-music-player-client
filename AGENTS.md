# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Package Manager

Use `yarn` instead of `npm` for installing packages and executing scripts.
